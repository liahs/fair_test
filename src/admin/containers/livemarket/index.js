import { Background, Header, LiveMarket } from "../../components";

export default function LiveMarketContainer() {
    return (
        <Background>
            <Header />
            <LiveMarket />
        </Background>
    )
}